{
  "name": "orch-agents",
  "version": "0.2.0",
  "description": "Autonomous Development Agency System - orchestrates AI agent swarms for software development workflows",
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node --env-file=.env dist/index.js",
    "test": "node --test tests/tech-lead-router.test.cjs && find tests -name '*.test.ts' -not -path 'tests/staging/*' -print0 | xargs -0 node --import tsx --test",
    "test:ts": "find tests -name '*.test.ts' -not -path 'tests/staging/*' -print0 | xargs -0 node --import tsx --test",
    "test:cjs": "node --test tests/**/*.test.cjs",
    "test:all": "node --test tests/**/*.test.cjs && node --import tsx --test tests/**/*.test.ts",
    "test:integration": "node --import tsx --test tests/integration/*.test.ts",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "typecheck": "tsc --noEmit",
    "setup": "node --import tsx src/setup/cli.ts"
  },
  "keywords": [
    "orchestration",
    "agents",
    "sparc",
    "claude-flow",
    "autonomous-development"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.22.0",
    "@types/node": "^22.19.15",
    "eslint": "^9.22.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.27.0"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.86",
    "fastify": "^5.8.2",
    "yaml": "^2.8.3"
  }
}
